{% extends 'base.html.twig' %}

{% block title %}Hello PinController!{% endblock %}

{# {% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/Cfitech/pinterest_dev/src/Controller/PinController.php'|file_link(0) }}">src/Controller/PinController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Cfitech/pinterest_dev/templates/pin/index.html.twig'|file_link(0) }}">templates/pin/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %} #}

{% block body %}
    {# {% set pin_count = pins|length %}
    {{ pin_count }} #}
    <li><a href="{{ path('app_home') }}">Home</a></li>
    <br>
    <li><a href="{{ path('app_pin_create') }}">Create Pin</a></li>

    {% for pin in pins %}
        <article>
               {# <h1>{{ pin.getTitle() }}</h1>   #}
               <h2><a href="{{ path('app_pin_show', {id: pin.id}) }}">{{ pin.title }} </a></h2>  

               <p>{{ pin.description|u.truncate(10,'...',false) }}</p> 
               <p>Submitted {{ pin.createdAt|ago }}</p>
        </article>
    {% else %}
        <p> Sorry, no pin yet </p>
    {% endfor %}
{% endblock %}
